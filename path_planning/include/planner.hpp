#pragma once

// =====================================================================
//  planner.hpp
//  ★ 사용자와 환경 사이의 유일한 약속(인터페이스) ★
//
//  당신이 개발할 경로 알고리즘은 아래 함수 하나로 표현됩니다.
//  이 함수의 "선언"만 환경이 알고 있고, "구현(로직)"은
//  user/my_planner.cpp 에서 당신이 직접 작성합니다.
//
//  환경(테스트 하네스)은 이 함수를 호출해서 결과 경로를 받고,
//  그것을 검증·시각화·측정합니다. 즉, 당신은 이 함수 본문만
//  채우면 나머지는 자동으로 돌아갑니다.
// =====================================================================

#include "grid.hpp"

namespace planning {

// [당신이 구현할 함수]
//   grid  : 장애물이 표시된 2D 환경
//   start : 출발 칸
//   goal  : 목표 칸
//
// 반환값(Path)에 대한 약속:
//   - 성공 시: start 로 시작해 goal 로 끝나는 칸들의 목록.
//             인접한 칸끼리는 한 스텝(상하좌우 또는 대각선 8방향)만
//             떨어져 있어야 하고, 어떤 칸도 장애물이면 안 됩니다.
//   - 실패/경로 없음: 빈 Path( {} )를 반환하세요.
//
// 검증 규칙의 세부 사항은 include/validator.hpp 를 참고하세요.
Path planPath(const Grid& grid, Cell start, Cell goal);

} // namespace planning
